#include<bits/stdc++.h>
using namespace std;
int main(){
    string s;
    getline(cin,s);
    reverse(s.begin(), s.end()); //reverse the given string
    cout<<s<<endl;
    return 0;
}// otherwise like reverse of array.
//T.C:O(n) & S.C:O(1)
